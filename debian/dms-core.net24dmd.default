# defaults file for net24dmd

# start net24dmd from init.d script?
#  only allowed values are "true", and "false"
NET24DMD_ENABLE=false

# Arguments for net24dmd
# it may be usefule to start with a different config file
#DAEMON_ARGS="-c /etc/net24/net24.conf"

